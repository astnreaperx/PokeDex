<h1>Type#index</h1>
<p>Find me in app/views/type/index.html.erb</p>


<table>
 <% @types.each do |t| %>
    <tr>
      <td><%= t.name %></td>
      <td><%= link_to 'Show', type_path(t) %></td>
    </tr>
  <% end %>
</table>
