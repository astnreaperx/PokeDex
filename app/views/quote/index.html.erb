<h1>Quote#index</h1>
<p>Find me in app/views/quote/index.html.erb</p>


<table>
 <% @quotes.each do |q| %>
    <tr>
      <td><%= q.quote %></td>
      <td><%= q.author %></td>
      <td><%= link_to 'Show', quote_path(q) %></td>
    </tr>
  <% end %>
</table>
